use std::collections::HashMap;

fn main() {
    let open_close_pairs = HashMap::from([('(', ')'), ('{', '}'), ('[', ']')]);
}

fn are_Parenthesis_Valid(before: String, string: Vec<String>) -> bool {
    return false;
}
